import cv2
import numpy as np


class merge:
    def run(idx, img, file_name, _input):

        # print(img_list)
        merge_img = img
        # for i in img_list:
        #     print(img_list[i]['img'])
        #     merge_img = np.concatenate(
        #         (merge_img, img_list[i]['img'][:, None]), axis=0)
        #     print("============================")

        # cv2.imshow("img_"+str(idx), merge_img)
        return img
